---
import {
  Smartphone,
  Globe,
  Database,
  Plug,
  Server,
} from "lucide-react";
import TechnologyTag from "../projects/TechnologyTag";
interface Props {
  lang: string;
  t: (key: string) => string;
}
const { t = (k: string) => k } = Astro.props as Props;

const gradients = [
  "from-blue-400 via-indigo-500 to-purple-500",
  "from-emerald-400 via-teal-400 to-cyan-500",
  "from-pink-400 via-rose-400 to-yellow-400",
  "from-orange-400 via-amber-400 to-yellow-500",
];

const skills = [
  {
    name: "Mobile Development",
    icon: Smartphone,
    technologies: ["Flutter", "Dart", "React Native", "FlutterFlow"],
    gradient: gradients[0],
  },
  {
    name: "Web Technologies",
    icon: Globe,
    technologies: [
      "React",
      "Nextjs",
      "Astro",
      "TailwindCSS",
      "Shadcn",
      "Typescript",
      "Vite",
      "Wordpress",
      "Laravel",
    ],
    gradient: gradients[1],
  },
  {
    name: "Backend & AI",
    icon: Database,
    technologies: [
      "Python",
      "Nodejs",
      "PHP",
      "Firebase",
      "MongoDB",
      "MySQL",
      "PostgreSQL",
      "FastAPI",
      "Redis",
      "RAG",
      "Vector DB",
      "AI Agents",
      "LLM",
      "OpenAI",
      "DeepSeek API",
    ],
    gradient: gradients[2],
  },
  {
    name: "Integrations",
    icon: Plug,
    technologies: [
      "WhatsApp Cloud API",
      "REST APIs",
      "Webhooks",
      "Telegram Bots",
      "PDF Generation",
      "OAuth",
    ],
    gradient: gradients[3],
  },
  {
    name: "Dev Ops",
    icon: Server,
    technologies: [
      "Linux (Arch)",
      "Docker",
      "Vercel",
      "Render",
      "Git",
      "GitHub Actions",
    ],
    gradient: gradients[0],
  },
];
---

<div class="container-content">
  <div class="text-center mb-10">
    <h2 class="type-h2 mb-2 bg-gradient-to-r from-foreground to-foreground/70 bg-clip-text text-transparent">{t('professional_experience')}</h2>
    <p class="mt-2 max-w-2xl mx-auto type-lead text-muted-foreground">{t('experience_description_1')}</p>
    <p class="mt-2 max-w-2xl mx-auto type-lead text-muted-foreground">{t('experience_description_2')}</p>
  </div>

  <!-- Skills grid -->
  <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6 items-stretch">
    {skills.map((skill) => (
      <div class="theme-light neomorphic group text-foreground flex flex-col gap-4 text-center p-6">
        <div class="w-20 h-20 rounded-full mx-auto mb-3 flex items-center justify-center transition-transform duration-300 group-hover:scale-105" style="background: linear-gradient(135deg, var(--tw-gradient-stops));">
          <div class={`w-12 h-12 rounded-full flex items-center justify-center text-white bg-gradient-to-br ${skill.gradient}`}>
            <skill.icon className="h-6 w-6" />
          </div>
        </div>
        <h3 class="font-semibold text-lg mb-1">{skill.name}</h3>
        <div class="flex flex-wrap justify-center gap-2">
          {((skill.technologies || []) as string[]).map((tech: string) => (
            <TechnologyTag tech={tech} />
          ))}
        </div>
      </div>
    ))}
  </div>

  <!-- Experience timeline 
  <div class="mt-10">
    <ol class="space-y-6">
      {experiences.map((exp) => (
        <li class="bg-white/90 p-6 rounded-2xl shadow-sm">
          <div class="flex items-center justify-between mb-2">
            <div>
              <h4 class="font-semibold">{exp.title} — <span class="font-normal">{exp.company}</span></h4>
              <time class="text-sm text-muted-foreground">{exp.year}</time>
            </div>
          </div>
          <p class="mt-3 text-muted-foreground">{t(exp.descriptionKey)}</p>
        </li>
      ))}
    </ol>
  </div>-->

</div>

