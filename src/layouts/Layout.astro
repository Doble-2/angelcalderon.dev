---
import Contact from "../components/Contact.astro";
import NavBar from "../components/share/NavBar.astro";
import Rainy from "../components/share/Rainy.astro";
import translations from "../i18n";
import SpeedInsights from "@vercel/speed-insights/astro";
import { onMount } from "solid-js";
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";

interface Props {
	title: string;
}

const { title } = Astro.props;
const firebaseConfig = {
	apiKey: "YOUR_API_KEY",
	authDomain: "YOUR_AUTH_DOMAIN",
	projectId: "YOUR_PROJECT_ID",
	storageBucket: "YOUR_STORAGE_BUCKET",
	messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
	appId: "YOUR_APP_ID",
	measurementId: "YOUR_MEASUREMENT_ID",
};

// Idioma determinado en el servidor mediante cookie o Accept-Language
let lang: string = 'es';
const cookieHeader = Astro.request.headers.get('cookie') || '';
const match = cookieHeader.match(/(?:^|;\s*)lang=([^;]+)/);
if (match) { try { lang = decodeURIComponent(match[1]); } catch {} }
else {
	const accept = Astro.request.headers.get('accept-language') || '';
	if (accept.toLowerCase().startsWith('en')) lang = 'en';
}
if (!['es','en'].includes(lang)) lang = 'es';
const t = (key: string) => translations[lang]?.[key] || key;

onMount(() => {
	const app = initializeApp(firebaseConfig);
	getAnalytics(app);
});
---

<!doctype html>
<html lang={lang} data-current-lang={lang}>
	<head>
		<meta charset="UTF-8" />
		<meta
			name="description"
			content="Programador con experiencia en la creación de sitios web y aplicaciones móviles. Especializado en desarrollo frontend con tecnologías como Flutter, Dart, React, Astro, Next.js y TypeScript. | este es mi portfolio, descubre mis proyectos y ponte en contacto conmigo"
		/>
		<meta property="twitter:image" content="/profile.png">
		<meta property="twitter:title" content={title}>
		<meta property="twitter:description" content="Programador con experiencia en la creación de sitios web y aplicaciones móviles. Especializado en desarrollo frontend con tecnologías como Flutter, Dart, React, Astro, Next.js y TypeScript. | este es mi portfolio, descubre mis proyectos y ponte en contacto conmigo">
		<meta property="og:image" content="/profile.png">
		<meta property="og:title" content={title}>
		<meta property="og:description" content="Programador con experiencia en la creación de sitios web y aplicaciones móviles. Especializado en desarrollo frontend con tecnologías como Flutter, Dart, React, Astro, Next.js y TypeScript. | este es mi portfolio, descubre mis proyectos y ponte en contacto conmigo">
		<meta property="og:type" content="website">
		<meta property="og:url" content="/">
		<meta property="og:site_name" content="Angel Calderon">
		<meta name="twitter:card" content="summary_large_image">
		<meta name="twitter:site" content="@angelcaldev">
		<meta name="twitter:creator" content="@angelcaldev">
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" href="/rect1-optimized.webp" />
		<link rel="canonical" href="/" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="max-w-screen">
		<Rainy />

		<NavBar lang={lang} t={t} />
		<main class="">
			<div
				class="z-10 main-section backdrop-blur-md bg-black/30"
			>
				<slot  />
				<Contact />
			</div>

			<!-- <div class="fixed bottom-4 right-4 z-30">
				<button
					id="rain-btn"
					class="bg-blue-500 hover:bg-blue-600 rounded-full shadow-lg inline-flex items-center justify-center focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none h-10 w-10"
					type="button"
					id="radix-:r2:"
					aria-haspopup="menu"
					aria-expanded="true"
					data-state="open"
					aria-controls="radix-:r3:"
				>
					<Image
						width="50"
						height="50"
						loading="lazy"
						class="w-8 h-8"
						src="https://img.icons8.com/ios/50/ffffff/blur.png"
						alt="blur"
					/>

					<path d="M5 12h14"></path>
					<path d="M12 5v14"></path>
				</button>
			</div> -->
		</main>
		<SpeedInsights />
	</body>
</html>
<script is:inline>
// Tema
function setTheme(theme) {
	if (theme === 'system') {
		window.localStorage.removeItem('theme');
		document.documentElement.removeAttribute('data-theme');
	} else {
		window.localStorage.setItem('theme', theme);
		document.documentElement.setAttribute('data-theme', theme);
	}
	if (theme === 'dark' || (theme === 'system' && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
		document.documentElement.classList.add('dark');
	} else {
		document.documentElement.classList.remove('dark');
	}
}
function getTheme() {
	return window.localStorage.getItem('theme') || 'system';
}
// Idioma
function setLanguage(lang) {
	document.cookie = `lang=${lang}; path=/;`;
	window.localStorage.setItem('lang', lang);
	location.reload();
}
function getLanguage() {
	return window.localStorage.getItem('lang') || 'es';
}
document.addEventListener('DOMContentLoaded', () => {
	setTheme(getTheme());
	// Si el idioma de cookie y localStorage no coinciden, sincroniza y recarga
	const cookieLang = document.cookie.match(/lang=([^;]+)/);
	const localLang = getLanguage();
	if (cookieLang && cookieLang[1] !== localLang) {
		setLanguage(localLang);
	}
});
</script>

<style is:global>
	:root {
		--accent: 136, 58, 234;
		--accent-light: 224, 204, 250;
		--accent-dark: 49, 10, 101;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
	}
	html {
		font-family: system-ui, sans-serif;
		background: #13151a;
	}
	code {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace;
	}
	body {
		margin: 0;
	}
</style>
